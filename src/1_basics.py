import time

# Nearly all programming languages, including Python, incorporate the concept of Unix time.
# Python’s standard library includes a module called time that can
# print the number of seconds since the Unix epoch:

print(f'current time in UNIX format: {time.time()}')
